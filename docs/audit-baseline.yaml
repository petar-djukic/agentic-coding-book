# audit-baseline.yaml -- findings mage audit reports but does not fail on (GH-27).
#
# The drafted chapters were written before the constitutions existed, so they
# predate the rules they are now measured against. Failing the build on that
# history would make the gate red from the day it shipped, and a gate that is
# always red gets turned off. Instead the debt is written down: each entry
# names the finding exactly and the issue that clears it.
#
# This is not an exemption list. Every entry is printed on each run, an entry
# without an `issue` is itself a finding, and an entry that no longer matches
# anything is reported as stale so the file cannot quietly outlive the defect.
# Delete entries as they are fixed; do not add one without an issue behind it.
#
# The `file`, `rule`, and `detail` fields must match a finding exactly -- run
# `mage audit` and copy the reported text.
#
# GH-12 cleared ten of the original thirteen: eight retired sidebar labels were
# renamed, and two findings against the old opening chapter left the audit's
# scope when that chapter was parked outside the numbered sequence as
# unplaced-intent-autonomy-and-verification.md. Those two defects still exist in
# that prose and are still tracked by GH-28; they will reappear here if the
# chapter is ever placed back into the book.

accepted:
  # --- Prose written before voice.yaml existed (GH-28) ---

  - file: 07-how-the-llm-works-and-fails.md
    rule: 'voice.yaml: forbidden_terms'
    detail: 'uses "just"'
    issue: GH-28
    note: >
      Inside the quoted phrase "just guesses randomly" -- a naive belief the
      chapter is reporting, not the minimizer the rule bars. May survive as a
      permanent exception; GH-28 decides.

  - file: 11-language-selection.md
    rule: 'voice.yaml: forbidden_terms'
    detail: 'uses "powerful"'
    issue: GH-28
    note: '"C++ is a powerful language precisely because it offers choice."'

  - file: 10-layered-construction.md
    rule: 'voice.yaml: figures.checks'
    detail: Figure 7.1 is never referenced from the prose
    issue: GH-28
    note: >
      Renumbered from Figure 2.1 by GH-35; the figure is still unreferenced,
      which is the part GH-28 clears.

  - file: 03-what-is-an-agent.md
    rule: 'voice.yaml: sidebars V-B4 authorship'
    detail: '"From the Field" sidebar #1 was last touched by the do-work skill; V-B4 is author_only'
    issue: GH-44
    note: >
      Section 1.3, on the skeleton becoming visible while rebuilding an agent.
      Written by an agent during GH-6 because srd-1.1 required the apparatus,
      and grounded in the declarative-agents work but not in a verified
      incident. GH-44 replaces it with the real one or cuts it.

  - file: 08-externalizing-memory.md
    rule: 'voice.yaml: sidebars V-B4 authorship'
    detail: '"From the Field" sidebar #1 was last touched by the do-work skill; V-B4 is author_only'
    issue: GH-44
    note: >
      Section 6.3, on a generation run that rebuilt a component against
      different assumptions. Same provenance, from GH-11.

  # The GH-25 chapter-to-SRD binding exposed six contract gaps on its first run.
  # Each needs a direction chosen -- spec wrong, or chapter wrong -- which is
  # GH-50's job. Recorded rather than fixed so the binding lands on its own.
  - file: 03-what-is-an-agent.md
    rule: GH-25 binding
    detail: docs/srd/srd-1.1-what-is-an-agent.yaml lists key term "agentic_loop", absent from the Key Terms table
    issue: GH-50
    note: >
      Likely a spec error: definitions.yaml marks agentic_loop introduced at
      C1.4, and C1.1 defers the loop to the harness chapters deliberately.

  - file: 05-what-is-a-harness.md
    rule: GH-25 binding
    detail: docs/srd/srd-1.3-what-is-a-harness.yaml lists key term "agentic_loop", absent from the Key Terms table
    issue: GH-50
    note: Same as C1.1; C1.3 names loop control without defining the loop.

  - file: 07-how-the-llm-works-and-fails.md
    rule: GH-25 binding
    detail: docs/srd/srd-1.5-llm-works-and-fails.yaml lists key term "interpolation_density", absent from the Key Terms table
    issue: GH-50
    note: Likely a chapter gap; the term is defined and the chapter is about interpolation.

  - file: 08-externalizing-memory.md
    rule: GH-25 binding
    detail: docs/srd/srd-1.6-externalizing-memory.yaml lists key term "knowledge_manager", absent from the Key Terms table
    issue: GH-50
    note: >
      Direction unclear: definitions.yaml marks knowledge_manager introduced at
      P5, so C1.6 may be reaching forward.

  - file: 04-the-agents-you-already-use.md
    rule: GH-25 binding
    detail: docs/srd/srd-1.2-agents-you-already-use.yaml names latentpatterns-harness as an anchor citation, which the chapter never cites
    issue: GH-50
    note: Chapter gap; it uses the harness vocabulary without crediting the source.

  - file: 05-what-is-a-harness.md
    rule: GH-25 binding
    detail: docs/srd/srd-1.3-what-is-a-harness.yaml names latentpatterns-harness as an anchor citation, which the chapter never cites
    issue: GH-50
    note: >
      Chapter gap, and the sharpest one: srd-1.3 calls this source the
      definitional anchor that carries the chapter, and section 3.1's boxed
      definition is what it should support.
